import { IData } from "./data";
export declare class Media {
    private map;
    constructor();
    getMedia(key: string): IData;
    addMedia(key: string, filePath: string): void;
    readonly array: IData[];
}
